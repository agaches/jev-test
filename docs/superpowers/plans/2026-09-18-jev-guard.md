# jev-guard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer les deux hooks `PreToolUse` de sécurité de Claude Code par un hook unique qui délègue le jugement à Jev, avec pré-filtre anti-exfiltration local, repli sur les expressions régulières actuelles, et déploiement en mode ombre.

**Architecture :** Un point d'entrée `hooks/jev-guard.sh` lit le JSON du hook sur stdin et enchaîne cinq étages : contrôles déterministes, pré-filtre anti-exfiltration, cache de verdicts, appel Jev, repli regex. Chaque étage est une bibliothèque shell autonome dans `lib/`, testable isolément. Le seuillage des probabilités reste en shell, jamais dans le modèle.

**Tech Stack :** bash, `curl`, `jq`, `awk` (comparaisons flottantes). Aucune dépendance nouvelle : tous ces outils sont déjà présents et déjà utilisés par les hooks existants.

**Spec :** `docs/superpowers/specs/2026-09-18-jev-security-hooks-design.md`

## Global Constraints

- **Aucune installation d'outil nouveau.** `curl`, `jq`, `awk`, `sed`, `grep` uniquement. (Instruction utilisateur globale : « toujours utiliser les outils et libraries déjà disponibles ».)
- **Aucun appel réseau dans les tests.** Le client Jev est stubé via la variable `JEV_GUARD_CURL`.
- **Jamais de commande en clair sur disque.** Ni dans le cache, ni dans le journal. Tout passe par `prefilter_redact` avant écriture.
- **Aucune ligne `Co-Authored-By` dans les messages de commit.** (Instruction utilisateur globale.)
- Endpoint : `https://api.typesafe.ai/v1/systemone`, surchargeable par `JEV_GUARD_ENDPOINT`.
- Alias de modèle par défaut : `jev-latest`, surchargeable par `JEV_GUARD_MODEL`.
- Timeout par défaut : `1500` ms, surchargeable par `JEV_GUARD_TIMEOUT_MS`.
- Mode par défaut : `shadow`. Une installation qui oublie de configurer le mode n'active jamais un blocage non mesuré.
- Seuils externalisés en variables d'environnement, jamais codés en dur (spec R5).
- Tout le code et tous les messages de commit sont en français, cohérents avec les hooks existants.

**Ajout au découpage de fichiers de la spec §5 :** le plan introduit `lib/decide.sh`, absent de l'arborescence de la spec. Le seuillage est une fonction pure sans entrée/sortie, c'est la pièce la plus critique pour la sécurité, et l'isoler la rend testable sans stub. `lib/jev-client.sh` ne garde que le transport.

---

### Task 1 : Harnais de test et pré-filtre anti-exfiltration

Le pré-filtre est la garantie centrale du design (spec §7) : il empêche qu'une commande contenant un secret soit envoyée à un tiers. Il n'a aucune dépendance, ce qui en fait le bon premier étage. Le harnais de test est replié dans cette tâche parce que c'est son premier consommateur.

**Files:**
- Create: `tests/lib/assert.sh`
- Create: `tests/run-tests.sh`
- Create: `lib/prefilter.sh`
- Create: `tests/test_prefilter.sh`
- Create: `.gitignore`
- Déjà présent : `.gitleaks.toml` (voir note ci-dessous)

**Note sur gitleaks.** Le dépôt est couvert par un hook `pre-commit` global qui
lance `gitleaks protect --staged`. Comme ce projet teste un détecteur de
secrets, ses tests contiennent par nature des valeurs ayant la forme de secrets.
Deux règles à respecter dans toutes les tâches :

1. Toute valeur de test est fabriquée **à entropie nulle** (répétitions de `A`,
   `0` ou `a`), jamais un faux secret d'apparence réaliste.
2. Le marqueur PEM est assemblé à l'exécution, jamais écrit en clair.

Le fichier `.gitleaks.toml` à la racine autorise exactement ces formes et rien
d'autre. Ne pas l'élargir, et ne jamais contourner le hook avec `--no-verify`.

**Interfaces:**
- Consumes: rien.
- Produces:
  - `prefilter_match <texte>` → code 0 et écrit le motif déclencheur sur stdout si un secret est détecté ; code 1 sinon.
  - `prefilter_redact <texte>` → écrit le texte avec chaque valeur de secret remplacée par `[REDACTED]`.
  - `assert_eq <attendu> <obtenu> <libellé>`, `assert_rc <code_attendu> <code_obtenu> <libellé>`, `finish` (harnais).

- [ ] **Step 1 : Créer le `.gitignore`**

```bash
cat > .gitignore <<'EOF'
*.log
.jev-guard-cache/
tests/tmp/
EOF
```

- [ ] **Step 2 : Écrire le harnais d'assertions**

Créer `tests/lib/assert.sh` :

```bash
#!/usr/bin/env bash
# Harnais d'assertions minimal. Aucune dépendance externe.

TESTS_RUN=0
TESTS_FAILED=0

assert_eq() {
  local attendu=$1 obtenu=$2 libelle=$3
  TESTS_RUN=$((TESTS_RUN + 1))
  if [ "$attendu" = "$obtenu" ]; then
    printf '  ok   %s\n' "$libelle"
  else
    TESTS_FAILED=$((TESTS_FAILED + 1))
    printf '  FAIL %s\n       attendu: [%s]\n       obtenu : [%s]\n' \
      "$libelle" "$attendu" "$obtenu"
  fi
}

assert_rc() {
  local attendu=$1 obtenu=$2 libelle=$3
  assert_eq "$attendu" "$obtenu" "$libelle (code retour)"
}

finish() {
  printf '  --- %d assertions, %d en échec\n' "$TESTS_RUN" "$TESTS_FAILED"
  [ "$TESTS_FAILED" -eq 0 ] || exit 1
  exit 0
}
```

- [ ] **Step 3 : Écrire le lanceur de tests**

Créer `tests/run-tests.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail

ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
export ROOT

echecs=0
for fichier in "$ROOT"/tests/test_*.sh; do
  [ -e "$fichier" ] || continue
  printf '\n== %s\n' "$(basename "$fichier")"
  bash "$fichier" || echecs=$((echecs + 1))
done

printf '\n'
if [ "$echecs" -gt 0 ]; then
  printf '%d fichier(s) de test en échec\n' "$echecs"
  exit 1
fi
printf 'Tous les tests passent\n'
```

Rendre exécutable : `chmod +x tests/run-tests.sh`

- [ ] **Step 4 : Écrire le test du pré-filtre (il doit échouer)**

Créer `tests/test_prefilter.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/prefilter.sh"

# Le marqueur PEM est assemblé à l'exécution : écrit en clair, il ferait
# échouer le hook gitleaks du dépôt sur un fichier de test légitime.
ESP=' '
PEM="-----BEGIN RSA PRIVATE${ESP}KEY-----"

# --- Valeurs de secrets : doivent déclencher ---
# Valeurs à entropie nulle (répétitions) pour rester distinguables d'un vrai
# secret par un lecteur comme par un scanner.
for secret in \
  'export OPENAI_API_KEY=sk-proj-AAAAAAAAAAAAAAAAAAAAAAAA' \
  'aws configure set aws_access_key_id AKIA0000000000000000' \
  'git remote add o https://ghp_000000000000000000000000000000000000@example.invalid/a/b' \
  'echo xoxb-000000000000-aaaaaaaaaa' \
  "echo \"$PEM\" > /tmp/k"
do
  prefilter_match "$secret" >/dev/null
  assert_rc 0 $? "détecte: ${secret:0:40}"
done

# --- Anodins : ne doivent PAS déclencher (faux positifs actuels) ---
for anodin in \
  'git show a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0' \
  'git cherry-pick a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0' \
  'curl "https://api.example.com/v1/data?token=public"' \
  'md5sum rapport.txt' \
  'export API_TOKEN="$SECRET_FROM_VAULT"' \
  'kubectl get secret ma-cle -o yaml'
do
  prefilter_match "$anodin" >/dev/null
  assert_rc 1 $? "laisse passer: ${anodin:0:40}"
done

# --- Expurgation ---
redige=$(prefilter_redact 'export K=sk-proj-AAAAAAAAAAAAAAAAAAAAAAAA suite')
assert_eq 'export K=[REDACTED] suite' "$redige" "expurge la valeur de clé"

intact=$(prefilter_redact 'git show a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0')
assert_eq 'git show a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0' "$intact" \
  "laisse un SHA git intact"

finish
```

- [ ] **Step 5 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/prefilter.sh: No such file or directory`

- [ ] **Step 6 : Écrire le pré-filtre**

Créer `lib/prefilter.sh` :

```bash
#!/usr/bin/env bash
# Pré-filtre anti-exfiltration (spec §7).
#
# Ne cible QUE des formats de VALEURS de secrets non ambigus. Jamais des noms
# de variables comme `token=`, sans quoi on réintroduirait le faux positif que
# tout ce chantier cherche à supprimer.
#
# Critère d'ajout d'un motif : il doit être impossible qu'il corresponde à un
# identifiant de commit, un hachage, un UUID ou une URL publique.

JEV_MOTIFS_SECRETS=(
  'sk-[A-Za-z0-9_-]{20,}'
  'AKIA[0-9A-Z]{16}'
  'ghp_[A-Za-z0-9]{36}'
  'xox[baprs]-[A-Za-z0-9-]{10,}'
  '-----BEGIN [A-Z ]*PRIVATE KEY-----'
)

prefilter_match() {
  local texte=$1 motif
  for motif in "${JEV_MOTIFS_SECRETS[@]}"; do
    if printf '%s' "$texte" | grep -qE "$motif"; then
      printf '%s' "$motif"
      return 0
    fi
  done
  return 1
}

prefilter_redact() {
  local texte=$1 motif
  for motif in "${JEV_MOTIFS_SECRETS[@]}"; do
    texte=$(printf '%s' "$texte" | sed -E "s|$motif|[REDACTED]|g")
  done
  printf '%s' "$texte"
}
```

- [ ] **Step 7 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 8 : Commit**

```bash
git add .gitignore tests/lib/assert.sh tests/run-tests.sh lib/prefilter.sh tests/test_prefilter.sh
git commit -m "Pré-filtre anti-exfiltration et harnais de test

Bloque localement les valeurs de secrets à format non ambigu avant tout
appel réseau, pour que le hook n'exfiltre jamais le secret qu'il détecte."
```

---

### Task 2 : Journal des décisions

Le journal est ce qui rend la phase A exploitable (spec §9, §10). Sans lui, aucun critère de passage en mode actif n'est mesurable.

**Files:**
- Create: `lib/log.sh`
- Create: `tests/test_log.sh`

**Interfaces:**
- Consumes: `prefilter_redact` (Task 1).
- Produces : `log_decision <tool> <cmd> <source> <verdict> <regex_verdict> <scores_json> <confidence> <latency_ms>` → ajoute une ligne JSON à `$JEV_GUARD_LOG`. Aucune sortie standard.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_log.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/prefilter.sh"
. "$ROOT/lib/log.sh"

tmp=$(mktemp -d)
trap 'rm -rf "$tmp"' EXIT
export JEV_GUARD_LOG="$tmp/sous/rep/jev-guard.jsonl"

log_decision "Bash" "git status" "jev" "allow" "allow" \
  '{"destructiveness":0.1}' "0.95" "118"

assert_eq "1" "$(wc -l < "$JEV_GUARD_LOG" | tr -d ' ')" "une ligne écrite"
assert_eq "0" "$(jq -e . "$JEV_GUARD_LOG" >/dev/null 2>&1; echo $?)" "JSON valide"
assert_eq "Bash"  "$(jq -r '.tool' "$JEV_GUARD_LOG")"    "champ tool"
assert_eq "allow" "$(jq -r '.verdict' "$JEV_GUARD_LOG")" "champ verdict"
assert_eq "true"  "$(jq -r '.agreed' "$JEV_GUARD_LOG")"  "accord calculé"
assert_eq "118"   "$(jq -r '.latency_ms' "$JEV_GUARD_LOG")" "latence"

# Le désaccord est calculé, pas fourni
log_decision "Bash" "rm -rf /tmp/x" "jev" "allow" "block" '{}' "0.5" "90"
assert_eq "false" "$(jq -r '.agreed' "$JEV_GUARD_LOG" | tail -1)" "désaccord détecté"

# Le journal ne doit JAMAIS contenir un secret en clair
log_decision "Bash" 'export K=sk-proj-AAAAAAAAAAAAAAAAAAAAAAAA' \
  "prefilter" "block" "block" '{}' "1.0" "0"
derniere=$(tail -1 "$JEV_GUARD_LOG")
assert_eq "0" "$(grep -c 'sk-proj-AAAAAAAAAAAAAAAAAAAAAAAA' <<<"$derniere")" \
  "aucun secret en clair dans le journal"
assert_eq "export K=[REDACTED]" "$(jq -r '.cmd_redacted' <<<"$derniere")" \
  "commande expurgée"

finish
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/log.sh: No such file or directory`

- [ ] **Step 3 : Écrire le journal**

Créer `lib/log.sh` :

```bash
#!/usr/bin/env bash
# Journal JSONL des décisions (spec §9).
# Dépend de prefilter_redact : aucune commande ne part en clair sur disque.

JEV_GUARD_LOG=${JEV_GUARD_LOG:-$HOME/.claude/logs/jev-guard.jsonl}

_sha256() {
  if command -v shasum >/dev/null 2>&1; then
    shasum -a 256 | cut -d' ' -f1
  else
    sha256sum | cut -d' ' -f1
  fi
}

log_decision() {
  local tool=$1 cmd=$2 source=$3 verdict=$4 regex_verdict=$5
  local scores=$6 confidence=$7 latency=$8

  mkdir -p "$(dirname "$JEV_GUARD_LOG")" 2>/dev/null || return 0

  local redige somme accord
  redige=$(prefilter_redact "$cmd")
  somme=$(printf '%s' "$cmd" | _sha256)
  if [ "$verdict" = "$regex_verdict" ]; then accord=true; else accord=false; fi

  jq -nc \
    --arg  ts            "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
    --arg  tool          "$tool" \
    --arg  cmd_sha256    "$somme" \
    --arg  cmd_redacted  "$redige" \
    --arg  source        "$source" \
    --arg  verdict       "$verdict" \
    --arg  regex_verdict "$regex_verdict" \
    --argjson agreed     "$accord" \
    --argjson scores     "${scores:-null}" \
    --argjson confidence "${confidence:-null}" \
    --argjson latency_ms "${latency:-null}" \
    '{$ts,$tool,$cmd_sha256,$cmd_redacted,$source,$verdict,
      $regex_verdict,$agreed,$scores,$confidence,$latency_ms}' \
    >> "$JEV_GUARD_LOG" 2>/dev/null || true
}
```

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add lib/log.sh tests/test_log.sh
git commit -m "Journal JSONL des décisions

Enregistre verdict, source, probabilités et latence pour rendre les
critères de passage en mode actif mesurables. La commande est expurgée
avant écriture."
```

---

### Task 3 : Cache de verdicts

Sans cache, une même commande répétée dans une session paie la latence et le coût à chaque fois. Le cache est consulté **après** le pré-filtre, pour qu'un secret ne puisse jamais être servi depuis le cache sans repasser par le blocage (spec §9).

**Files:**
- Create: `lib/cache.sh`
- Create: `tests/test_cache.sh`

**Interfaces:**
- Consumes: `_sha256` (Task 2, via `lib/log.sh`).
- Produces :
  - `cache_key <tool> <sujet> <cwd>` → écrit la clé hexadécimale sur stdout.
  - `cache_get <clé>` → écrit le verdict sur stdout et retourne 0 si présent et non expiré ; retourne 1 sinon.
  - `cache_put <clé> <verdict> <scores_json>` → écrit l'entrée.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_cache.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/prefilter.sh"
. "$ROOT/lib/log.sh"
. "$ROOT/lib/cache.sh"

tmp=$(mktemp -d)
trap 'rm -rf "$tmp"' EXIT
export JEV_GUARD_CACHE_DIR="$tmp/cache"

cle=$(cache_key "Bash" "git status" "/projet")
assert_eq "64" "${#cle}" "clé sha256 de 64 caractères"

cle2=$(cache_key "Bash" "git status" "/autre")
assert_eq "0" "$([ "$cle" != "$cle2" ]; echo $?)" "le cwd change la clé"

cache_get "$cle" >/dev/null
assert_rc 1 $? "miss sur cache vide"

cache_put "$cle" "allow" '{"destructiveness":0.1}'
assert_eq "allow" "$(cache_get "$cle")" "hit après écriture"

# Entrée expirée : ts_epoch il y a 8 jours
vieux=$(cache_key "Bash" "vieille commande" "/projet")
mkdir -p "$JEV_GUARD_CACHE_DIR"
jq -nc --argjson ts "$(( $(date +%s) - 8*24*3600 ))" \
  '{verdict:"allow",scores:{},ts_epoch:$ts}' \
  > "$JEV_GUARD_CACHE_DIR/$vieux.json"
cache_get "$vieux" >/dev/null
assert_rc 1 $? "miss sur entrée expirée"
assert_eq "0" "$([ ! -f "$JEV_GUARD_CACHE_DIR/$vieux.json" ]; echo $?)" \
  "entrée expirée supprimée"

# Aucune commande en clair dans le cache
assert_eq "0" "$(grep -rl 'git status' "$JEV_GUARD_CACHE_DIR" 2>/dev/null | wc -l | tr -d ' ')" \
  "aucune commande en clair dans le cache"

finish
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/cache.sh: No such file or directory`

- [ ] **Step 3 : Écrire le cache**

Créer `lib/cache.sh` :

```bash
#!/usr/bin/env bash
# Cache de verdicts sur disque (spec §9).
# Indexé par (outil, sujet, cwd). Ne stocke jamais le sujet en clair.

JEV_GUARD_CACHE_DIR=${JEV_GUARD_CACHE_DIR:-${XDG_CACHE_HOME:-$HOME/.cache}/jev-guard}
JEV_CACHE_TTL=${JEV_CACHE_TTL:-604800}   # 7 jours

cache_key() {
  printf '%s\0%s\0%s' "$1" "$2" "$3" | _sha256
}

cache_get() {
  local fichier="$JEV_GUARD_CACHE_DIR/$1.json" ts maintenant
  [ -f "$fichier" ] || return 1
  ts=$(jq -r '.ts_epoch // 0' "$fichier" 2>/dev/null) || return 1
  maintenant=$(date +%s)
  if [ $(( maintenant - ts )) -ge "$JEV_CACHE_TTL" ]; then
    rm -f "$fichier"
    return 1
  fi
  jq -r '.verdict' "$fichier"
}

cache_put() {
  mkdir -p "$JEV_GUARD_CACHE_DIR" 2>/dev/null || return 0
  jq -nc --arg verdict "$2" --argjson scores "${3:-null}" \
         --argjson ts "$(date +%s)" \
         '{$verdict,$scores,ts_epoch:$ts}' \
    > "$JEV_GUARD_CACHE_DIR/$1.json" 2>/dev/null || true
}
```

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add lib/cache.sh tests/test_cache.sh
git commit -m "Cache de verdicts indexé par outil, sujet et répertoire

Ramène le coût à un appel par sujet distinct. Le sujet n'est stocké que
sous forme de hachage."
```

---

### Task 4 : Repli sur les expressions régulières actuelles

Ce module reprend à l'identique la logique des deux hooks existants. C'est le plancher hors ligne : l'utilisateur ne doit jamais se retrouver moins protégé qu'avant ce chantier (spec §9). Ses faux positifs connus sont **conservés délibérément** et documentés par un test, pour que personne ne les « corrige » par inadvertance en croyant à un bug.

**Files:**
- Create: `lib/fallback.sh`
- Create: `tests/test_fallback.sh`

**Interfaces:**
- Consumes: rien.
- Produces : `fallback_verdict <tool> <sujet>` → écrit `block`, `ask` ou `allow` sur stdout.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_fallback.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/fallback.sh"

# --- Comportements à préserver ---
assert_eq block "$(fallback_verdict Bash 'rm -rf /')"              "rm -rf /"
assert_eq block "$(fallback_verdict Bash 'dd if=/dev/zero of=/dev/sda')" "dd"
assert_eq block "$(fallback_verdict Bash 'npm publish')"           "npm publish"
assert_eq block "$(fallback_verdict Bash 'git push --force origin main')" "force push main"
assert_eq block "$(fallback_verdict Bash 'export password=hunter2')" "motif password="
assert_eq block "$(fallback_verdict Write '/projet/.env')"          "fichier .env"
assert_eq block "$(fallback_verdict Edit '/projet/id_rsa')"         "clé privée"
assert_eq allow "$(fallback_verdict Bash 'ls -la')"                 "commande anodine"
assert_eq ask   "$(fallback_verdict Bash 'rm -r build/')"           "suppression récursive"

# --- Faux positifs CONSERVÉS volontairement (spec §9) ---
# Le repli reproduit le comportement actuel, défauts compris. C'est un
# plancher assumé, pas une régression. Ne pas « corriger » ces cas ici :
# c'est Jev qui les traite en mode nominal.
assert_eq block "$(fallback_verdict Bash 'git show a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0')" \
  "faux positif SHA git conservé en mode dégradé"
assert_eq block "$(fallback_verdict Bash 'curl "https://ex.com/d?token=public"')" \
  "faux positif token= conservé en mode dégradé"

finish
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/fallback.sh: No such file or directory`

- [ ] **Step 3 : Écrire le repli**

Créer `lib/fallback.sh` :

```bash
#!/usr/bin/env bash
# Repli en mode dégradé (spec §9).
#
# Reprend à l'identique les règles de dangerous-actions-blocker.sh et
# security-check.sh. Reproduit le comportement actuel, faux positifs compris.
# Ne pas « améliorer » ces règles : leur rôle est d'être un plancher stable et
# prévisible quand Jev est indisponible.

_FB_DANGEREUX=(
  'rm -rf /' 'rm -rf ~' 'rm -rf $HOME' 'dd if=' 'mkfs' ':(){:|:&};:'
  '> /dev/sda' 'chmod -R 777 /' 'chown -R' 'sudo rm'
  'DROP DATABASE' 'DROP TABLE' '--no-preserve-root'
)

_FB_SECRETS=(
  'password=' 'secret=' 'api_key=' 'apikey=' 'token='
  'aws_access_key' 'aws_secret' 'private_key'
)

_FB_FICHIERS=(
  '.env' '.env.local' '.env.production' '.env.development'
  'credentials.json' 'serviceAccountKey.json'
  'id_rsa' 'id_ed25519' 'id_ecdsa' '.npmrc' '.pypirc'
  'secrets.yml' 'secrets.yaml'
)

fallback_verdict() {
  local outil=$1 sujet=$2 motif

  if [ "$outil" = "Bash" ]; then
    for motif in "${_FB_DANGEREUX[@]}"; do
      [[ "$sujet" == *"$motif"* ]] && { printf block; return; }
    done
    if grep -qE 'git push.*(-f|--force).*(main|master)' <<<"$sujet"; then
      printf block; return
    fi
    if grep -qE 'npm publish|pnpm publish|yarn publish' <<<"$sujet"; then
      printf block; return
    fi
    for motif in "${_FB_SECRETS[@]}"; do
      grep -qi -- "$motif" <<<"$sujet" && { printf block; return; }
    done
    if grep -qE '(sk-[a-zA-Z0-9]{20,}|pk_[a-zA-Z0-9]{20,}|[a-f0-9]{32,})' <<<"$sujet"; then
      printf block; return
    fi
    if grep -qE 'rm -r|rmdir|unlink' <<<"$sujet"; then
      printf ask; return
    fi
    printf allow; return
  fi

  if [ "$outil" = "Edit" ] || [ "$outil" = "Write" ]; then
    local base; base=$(basename "$sujet")
    for motif in "${_FB_FICHIERS[@]}"; do
      [ "$base" = "$motif" ] && { printf block; return; }
    done
    printf allow; return
  fi

  printf allow
}
```

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add lib/fallback.sh tests/test_fallback.sh
git commit -m "Repli sur les règles regex actuelles

Reprend à l'identique la logique des deux hooks existants comme plancher
hors ligne. Les faux positifs connus sont conservés et verrouillés par des
tests : c'est le comportement d'aujourd'hui, pas une régression."
```

---

### Task 5 : Contrôles déterministes de chemin

Le confinement de chemin est une comparaison de préfixe exacte. Le confier à un modèle probabiliste n'ajouterait que du risque (spec §6). Ce module reste en pur shell et s'exécute avant tout appel réseau.

**Files:**
- Create: `lib/deterministic.sh`
- Create: `tests/test_deterministic.sh`

**Interfaces:**
- Consumes: rien.
- Produces : `path_containment_verdict <chemin>` → écrit `block` si le chemin sort des zones autorisées, `allow` sinon. Zones autorisées : `$CLAUDE_PROJECT_DIR` (défaut `$PWD`), `$HOME/.claude`, `/tmp`, plus les préfixes de `$ALLOWED_PATHS` séparés par `:`.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_deterministic.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/deterministic.sh"

export CLAUDE_PROJECT_DIR=/projet
unset ALLOWED_PATHS

assert_eq allow "$(path_containment_verdict /projet/src/a.ts)" "dans le projet"
assert_eq allow "$(path_containment_verdict "$HOME/.claude/settings.json")" "dans ~/.claude"
assert_eq allow "$(path_containment_verdict /tmp/scratch.txt)"  "dans /tmp"
assert_eq block "$(path_containment_verdict /etc/passwd)"       "hors zones autorisées"
assert_eq block "$(path_containment_verdict "$HOME/Documents/x")" "hors zones autorisées"

export ALLOWED_PATHS=/opt/data:/srv/www
assert_eq allow "$(path_containment_verdict /opt/data/f.csv)" "zone supplémentaire 1"
assert_eq allow "$(path_containment_verdict /srv/www/i.html)" "zone supplémentaire 2"
assert_eq block "$(path_containment_verdict /opt/autre/f)"    "hors zone supplémentaire"

finish
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/deterministic.sh: No such file or directory`

- [ ] **Step 3 : Écrire les contrôles déterministes**

Créer `lib/deterministic.sh` :

```bash
#!/usr/bin/env bash
# Contrôles déterministes de chemin (spec §6).
# Repris de dangerous-actions-blocker.sh:106-130. Comparaison de préfixe
# exacte : correcte, gratuite, aucun appel réseau.

path_containment_verdict() {
  local chemin=$1
  local projet="${CLAUDE_PROJECT_DIR:-$PWD}"
  local claude_home="$HOME/.claude"
  local autorise=1

  [[ "$chemin" == "$projet"* ]]       && autorise=0
  [[ "$chemin" == "$claude_home"* ]]  && autorise=0
  [[ "$chemin" == /tmp* ]]            && autorise=0

  if [ -n "${ALLOWED_PATHS:-}" ]; then
    local zone
    IFS=':' read -ra _zones <<< "$ALLOWED_PATHS"
    for zone in "${_zones[@]}"; do
      [ -n "$zone" ] && [[ "$chemin" == "$zone"* ]] && autorise=0
    done
  fi

  [ "$autorise" -eq 0 ] && printf allow || printf block
}
```

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add lib/deterministic.sh tests/test_deterministic.sh
git commit -m "Contrôles déterministes de confinement de chemin

Comparaison de préfixe exacte reprise du hook actuel. Reste hors du
périmètre de Jev : le code fait déjà ce contrôle correctement."
```

---

### Task 6 : Seuillage des probabilités

La pièce la plus critique pour la sécurité. Fonction pure, aucune entrée/sortie, donc testable exhaustivement sans stub. Implémente les cinq règles ordonnées de la spec §8.3.

**Files:**
- Create: `lib/decide.sh`
- Create: `tests/test_decide.sh`

**Interfaces:**
- Consumes: rien (`jq` et `awk` uniquement).
- Produces : `decide_bash <reponse_json>` → écrit `block`, `ask` ou `allow`. `decide_file <reponse_json>` → idem pour `Edit`/`Write`.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_decide.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/decide.sh"

reponse() {  # secret destructiveness blast conf_d conf_b
  jq -nc --argjson s "$1" --argjson d "$2" --arg b "$3" \
         --argjson cd "$4" --argjson cb "$5" '{
    answers: {
      secret_exposure: {noul: $s},
      destructiveness: {score: $d, confidence: $cd},
      blast_radius:    {choice: $b, confidence: $cb}
    }}'
}

# Règle 1 : secret
assert_eq block "$(decide_bash "$(reponse 0.90 0.0 cwd 1 1)")" "R1 secret au-dessus du seuil"
assert_eq allow "$(decide_bash "$(reponse 0.80 0.0 cwd 1 1)")" "R1 secret sous le seuil"

# Règle 2 : destructeur ET portée large
assert_eq block "$(decide_bash "$(reponse 0 2.6 shared_remote 1 1)")" "R2 distant partagé"
assert_eq block "$(decide_bash "$(reponse 0 3.0 machine 1 1)")"       "R2 machine"
assert_eq ask   "$(decide_bash "$(reponse 0 2.6 repo 1 1)")"          "R2 portée étroite → R3"

# Règle 3 : destructeur seul
assert_eq ask   "$(decide_bash "$(reponse 0 1.5 cwd 1 1)")" "R3 au seuil"
assert_eq allow "$(decide_bash "$(reponse 0 1.4 cwd 1 1)")" "R3 sous le seuil"

# Règle 4 : porte de confiance (ne peut qu'escalader allow → ask)
assert_eq ask   "$(decide_bash "$(reponse 0 0.2 cwd 0.55 1)")" "R4 confiance destructiveness basse"
assert_eq ask   "$(decide_bash "$(reponse 0 0.2 cwd 1 0.55)")" "R4 confiance blast basse"
assert_eq block "$(decide_bash "$(reponse 0.95 0.2 cwd 0.10 0.10)")" \
  "R4 n'annule jamais un blocage"

# Règle 5
assert_eq allow "$(decide_bash "$(reponse 0 0.1 cwd 0.99 0.99)")" "R5 cas nominal"

# Champs manquants : ne doit pas planter, défauts sûrs
assert_eq allow "$(decide_bash '{"answers":{}}')" "réponse vide → allow"

# Seuils surchargeables (spec R5)
JEV_T_SECRET=0.50 assert_eq block "$(JEV_T_SECRET=0.50 decide_bash "$(reponse 0.60 0 cwd 1 1)")" \
  "seuil secret surchargeable"

# Fichiers
assert_eq block "$(decide_file '{"answers":{"sensitive_file":{"noul":0.85}}}')" "fichier sensible"
assert_eq ask   "$(decide_file '{"answers":{"sensitive_file":{"noul":0.60}}}')" "fichier douteux"
assert_eq allow "$(decide_file '{"answers":{"sensitive_file":{"noul":0.20}}}')" "fichier anodin"

finish
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/decide.sh: No such file or directory`

- [ ] **Step 3 : Écrire le seuillage**

Créer `lib/decide.sh` :

```bash
#!/usr/bin/env bash
# Seuillage des probabilités (spec §8.3).
#
# Le modèle fournit des nombres, ce fichier produit la décision. Les cinq
# règles sont évaluées dans l'ordre, la première qui s'applique l'emporte.
# awk est utilisé parce que bash ne compare pas les flottants.

decide_bash() {
  local r=$1
  local secret destr blast conf_d conf_b
  secret=$(jq -r '.answers.secret_exposure.noul      // 0'     <<<"$r")
  destr=$( jq -r '.answers.destructiveness.score     // 0'     <<<"$r")
  blast=$( jq -r '.answers.blast_radius.choice       // "cwd"' <<<"$r")
  conf_d=$(jq -r '.answers.destructiveness.confidence // 1'    <<<"$r")
  conf_b=$(jq -r '.answers.blast_radius.confidence    // 1'    <<<"$r")

  awk -v s="$secret" -v d="$destr" -v b="$blast" \
      -v cd="$conf_d" -v cb="$conf_b" \
      -v ts="${JEV_T_SECRET:-0.85}" \
      -v tdb="${JEV_T_DESTRUCT_BLOCK:-2.5}" \
      -v tda="${JEV_T_DESTRUCT_ASK:-1.5}" \
      -v tc="${JEV_T_CONFIDENCE:-0.60}" '
    BEGIN {
      if (s > ts)                                                  { print "block"; exit }
      if (d >= tdb && (b == "machine" || b == "shared_remote"))     { print "block"; exit }
      if (d >= tda)                                                { print "ask";   exit }
      if (cd < tc || cb < tc)                                      { print "ask";   exit }
      print "allow"
    }'
}

decide_file() {
  local r=$1 sensible
  sensible=$(jq -r '.answers.sensitive_file.noul // 0' <<<"$r")
  awk -v v="$sensible" \
      -v tb="${JEV_T_FILE_BLOCK:-0.80}" \
      -v ta="${JEV_T_FILE_ASK:-0.50}" '
    BEGIN {
      if (v > tb) { print "block"; exit }
      if (v > ta) { print "ask";   exit }
      print "allow"
    }'
}
```

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add lib/decide.sh tests/test_decide.sh
git commit -m "Seuillage des probabilités en cinq règles ordonnées

Fonction pure, testée exhaustivement. La porte de confiance ne peut
qu'escalader allow vers ask, jamais adoucir un blocage."
```

---

### Task 7 : Client Jev

Transport uniquement : construction du payload, appel HTTP, validation de la réponse. Aucune décision. L'appel réel est injectable par `JEV_GUARD_CURL` pour que les tests ne touchent jamais le réseau.

**Files:**
- Create: `lib/jev-client.sh`
- Create: `tests/test_jev_client.sh`
- Create: `tests/stubs/curl-ok`
- Create: `tests/stubs/curl-500`
- Create: `tests/stubs/curl-timeout`

**Interfaces:**
- Consumes: rien.
- Produces :
  - `jev_build_bash_payload <commande> <cwd> <in_git_bool>` → écrit le corps JSON.
  - `jev_build_file_payload <chemin> <tool>` → écrit le corps JSON.
  - `jev_query <payload>` → écrit la réponse JSON et retourne 0 ; retourne 1 sur toute défaillance (timeout, HTTP non 2xx, JSON invalide, clé absente).

- [ ] **Step 1 : Confirmer le schéma HTTP réel**

La spec §8.1 signale que la forme exacte du corps JSON n'a pas été vérifiée : elle a été déduite des exemples SDK, pas lue depuis la référence HTTP.

Consulter `https://docs.typesafe.ai/api` et confirmer :
- le nom et l'imbrication des champs `state`, `questions`, `model` ;
- la façon dont le type d'une question est déclaré (champ `type` ? clé dédiée ?) ;
- le nom exact des champs de réponse : `.answers.<clé>.choice`, `.score`, `.noul`, `.probabilities`, `.confidence` ;
- l'en-tête d'authentification exact (`Authorization: Bearer` ?).

Si le schéma diffère de celui du Step 3, ajuster `jev_build_*_payload` et les chemins `jq` de `lib/decide.sh` (Task 6) **avant** d'écrire le code, puis relancer les tests de la Task 6.

- [ ] **Step 2 : Écrire les stubs curl**

Créer `tests/stubs/curl-ok` :

```bash
#!/usr/bin/env bash
# Stub curl : réponse Jev nominale. Ignore tous les arguments.
cat <<'EOF'
{"model":"jev-1.13.0","answers":{"secret_exposure":{"noul":0.02},"destructiveness":{"score":0.3,"confidence":0.94},"blast_radius":{"choice":"cwd","confidence":0.91}},"usage":{"input_tokens":120,"output_tokens":0}}
EOF
printf '\n200'
```

Créer `tests/stubs/curl-500` :

```bash
#!/usr/bin/env bash
printf '{"error":"internal"}\n500'
```

Créer `tests/stubs/curl-timeout` :

```bash
#!/usr/bin/env bash
exit 28   # code curl pour timeout
```

Rendre exécutables : `chmod +x tests/stubs/curl-*`

- [ ] **Step 3 : Écrire le test (il doit échouer)**

Créer `tests/test_jev_client.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"
. "$ROOT/lib/jev-client.sh"

export TYPESAFE_API_KEY=cle-de-test

# --- Construction du payload ---
p=$(jev_build_bash_payload 'git status' '/projet' true)
assert_eq "0" "$(jq -e . <<<"$p" >/dev/null 2>&1; echo $?)" "payload JSON valide"
assert_eq "jev-latest" "$(jq -r '.model' <<<"$p")"              "modèle par défaut"
assert_eq "git status" "$(jq -r '.state.command' <<<"$p")"      "commande dans l'état"
assert_eq "/projet"    "$(jq -r '.state.cwd' <<<"$p")"          "cwd dans l'état"
assert_eq "Bash"       "$(jq -r '.state.tool' <<<"$p")"         "outil dans l'état"
assert_eq "3" "$(jq -r '.questions | length' <<<"$p")"          "trois questions"
assert_eq "score"  "$(jq -r '.questions.destructiveness.type' <<<"$p")" "type score"
assert_eq "noul"   "$(jq -r '.questions.secret_exposure.type' <<<"$p")" "type noul"
assert_eq "choice" "$(jq -r '.questions.blast_radius.type' <<<"$p")"    "type choice"
assert_eq "4" "$(jq -r '.questions.destructiveness.criteria | length' <<<"$p")" \
  "rubrique à quatre niveaux"

pm=$(JEV_GUARD_MODEL=jev-1.13.0 jev_build_bash_payload 'ls' '/p' false)
assert_eq "jev-1.13.0" "$(jq -r '.model' <<<"$pm")" "modèle surchargeable"

pf=$(jev_build_file_payload '/projet/.env.staging' Write)
assert_eq "1" "$(jq -r '.questions | length' <<<"$pf")" "une seule question fichier"
assert_eq "noul" "$(jq -r '.questions.sensitive_file.type' <<<"$pf")" "type noul fichier"

# --- Transport ---
r=$(JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok" jev_query "$p")
assert_rc 0 $? "réponse 200 acceptée"
assert_eq "0.02" "$(jq -r '.answers.secret_exposure.noul' <<<"$r")" "réponse parsable"

JEV_GUARD_CURL="$ROOT/tests/stubs/curl-500" jev_query "$p" >/dev/null 2>&1
assert_rc 1 $? "HTTP 500 rejeté"

JEV_GUARD_CURL="$ROOT/tests/stubs/curl-timeout" jev_query "$p" >/dev/null 2>&1
assert_rc 1 $? "timeout rejeté"

( unset TYPESAFE_API_KEY
  JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok" jev_query "$p" >/dev/null 2>&1 )
assert_rc 1 $? "clé absente rejetée sans appel"

finish
```

- [ ] **Step 4 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `lib/jev-client.sh: No such file or directory`

- [ ] **Step 5 : Écrire le client**

Créer `lib/jev-client.sh` :

```bash
#!/usr/bin/env bash
# Transport vers l'API Jev (spec §8.1). Aucune décision ici.

JEV_GUARD_ENDPOINT=${JEV_GUARD_ENDPOINT:-https://api.typesafe.ai/v1/systemone}

_JEV_RUBRIQUE_DESTRUCTION='[
  "Aucune modification d'\''état : lecture, inspection, affichage, recherche.",
  "Modification réversible sans effort : création de fichier, écriture dans un fichier suivi par git, installation de dépendance locale.",
  "Perte possible de travail local non sauvegardé : reset, checkout destructif, stash drop, suppression de fichiers non suivis, réécriture d'\''historique local.",
  "Destruction irréversible ou effet hors de la machine : suppression récursive large, force-push, suppression de branche distante, drop de base, publication de paquet, écriture sur un périphérique bloc."
]'

_JEV_PORTEES='{
  "cwd": "L'\''effet reste dans le répertoire de travail courant.",
  "repo": "L'\''effet touche le dépôt local au-delà du répertoire courant.",
  "machine": "L'\''effet touche la machine hors du dépôt : système, paquets globaux, répertoire personnel, configuration utilisateur.",
  "shared_remote": "L'\''effet est visible par d'\''autres : dépôt distant, service déployé, base de données partagée, registre de paquets."
}'

# La seconde phrase de cette instruction est ce qui corrige le faux positif
# sur les SHA git : elle exclut explicitement la classe d'objets que la regex
# actuelle confond avec une clé d'API.
_JEV_INSTR_SECRET="La commande contient-elle la VALEUR d'un secret en clair (clé d'API, jeton, mot de passe, clé privée) ? Répondre non si la commande ne fait que nommer une variable d'environnement, lire un secret depuis un fichier ou un gestionnaire de secrets, ou contenir un identifiant de commit, un hachage ou un UUID."

jev_build_bash_payload() {
  jq -n \
    --arg model "${JEV_GUARD_MODEL:-jev-latest}" \
    --arg cmd "$1" --arg cwd "$2" --argjson git "$3" \
    --argjson rubrique "$_JEV_RUBRIQUE_DESTRUCTION" \
    --argjson portees "$_JEV_PORTEES" \
    --arg instr_secret "$_JEV_INSTR_SECRET" '
    {
      model: $model,
      state: { tool: "Bash", command: $cmd, cwd: $cwd, in_git_repo: $git },
      questions: {
        destructiveness: {
          type: "score",
          instructions: "À quel point cette commande shell est-elle destructrice ou irréversible ?",
          criteria: $rubrique
        },
        secret_exposure: { type: "noul", instructions: $instr_secret },
        blast_radius: {
          type: "choice",
          instructions: "Jusqu'\''où porte l'\''effet de cette commande ?",
          criteria: $portees
        }
      }
    }'
}

jev_build_file_payload() {
  jq -n \
    --arg model "${JEV_GUARD_MODEL:-jev-latest}" \
    --arg path "$1" --arg tool "$2" '
    {
      model: $model,
      state: { tool: $tool, path: $path },
      questions: {
        sensitive_file: {
          type: "noul",
          instructions: "Ce chemin désigne-t-il un fichier contenant des secrets, des identifiants, une clé privée ou une configuration de production sensible ?"
        }
      }
    }'
}

jev_query() {
  local payload=$1 brut code corps timeout_s
  [ -n "${TYPESAFE_API_KEY:-}" ] || return 1

  timeout_s=$(awk -v ms="${JEV_GUARD_TIMEOUT_MS:-1500}" 'BEGIN{printf "%.3f", ms/1000}')

  brut=$("${JEV_GUARD_CURL:-curl}" -sS -w '\n%{http_code}' \
      --max-time "$timeout_s" \
      -H "Authorization: Bearer $TYPESAFE_API_KEY" \
      -H 'Content-Type: application/json' \
      -d "$payload" "$JEV_GUARD_ENDPOINT" 2>/dev/null) || return 1

  code=${brut##*$'\n'}
  corps=${brut%$'\n'*}

  [[ "$code" =~ ^2[0-9][0-9]$ ]] || return 1
  jq -e . >/dev/null 2>&1 <<<"$corps" || return 1
  printf '%s' "$corps"
}
```

- [ ] **Step 6 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 7 : Commit**

```bash
git add lib/jev-client.sh tests/test_jev_client.sh tests/stubs/
git commit -m "Client Jev : construction du payload et transport HTTP

L'appel curl est injectable par JEV_GUARD_CURL, ce qui permet de tester
tous les modes de défaillance sans toucher le réseau."
```

---

### Task 8 : Orchestration du hook

Assemble les six étages et implémente le protocole de sortie des hooks Claude Code, plus la distinction mode ombre / mode actif.

**Files:**
- Create: `hooks/jev-guard.sh`
- Create: `tests/test_guard.sh`

**Interfaces:**
- Consumes: tous les modules des Tasks 1 à 7.
- Produces : exécutable lisant le JSON du hook sur stdin. Sortie : code 0 pour autoriser, code 2 + message sur stderr pour bloquer, JSON `hookSpecificOutput` sur stdout pour demander confirmation.

- [ ] **Step 1 : Confirmer le protocole de sortie « ask »**

Le mécanisme de blocage (code 2 + stderr) est attesté par les hooks existants (`dangerous-actions-blocker.sh:39`). Le mécanisme de demande de confirmation l'est moins : `rtk-rewrite.sh:77` obtient une invite en **omettant** `permissionDecision`, ce qui n'est pas la même chose qu'un `ask` explicite.

Vérifier dans la documentation des hooks Claude Code si `permissionDecision: "ask"` est une valeur acceptée. Si oui, l'utiliser. Sinon, produire le JSON sans `permissionDecision`, ce qui laisse Claude Code appliquer son flux de permission normal. Le test du Step 3 doit être aligné sur la réponse obtenue.

- [ ] **Step 2 : Écrire le test (il doit échouer)**

Créer `tests/test_guard.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"

tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT
export JEV_GUARD_LOG="$tmp/j.jsonl"
export JEV_GUARD_CACHE_DIR="$tmp/c"
export TYPESAFE_API_KEY=cle-de-test
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok"
export CLAUDE_PROJECT_DIR=/projet

entree() { jq -nc --arg t "$1" --argjson i "$2" '{tool_name:$t,tool_input:$i}'; }
lancer() { printf '%s' "$1" | bash "$ROOT/hooks/jev-guard.sh" >"$tmp/out" 2>"$tmp/err"; printf '%s' $?; }

export JEV_GUARD_MODE=active

# Stub curl-ok renvoie destructiveness 0.3 / secret 0.02 → allow
rc=$(lancer "$(entree Bash '{"command":"git status"}')")
assert_eq "0" "$rc" "commande anodine autorisée"

# Pré-filtre : blocage local, aucun appel réseau
export JEV_GUARD_CURL=/bin/false   # tout appel réseau ferait échouer le test
rc=$(lancer "$(entree Bash '{"command":"export K=sk-proj-AAAAAAAAAAAAAAAAAAAAAAAA"}')")
assert_eq "2" "$rc" "pré-filtre bloque"
assert_eq "prefilter" "$(jq -r '.source' "$JEV_GUARD_LOG" | tail -1)" \
  "source = prefilter, donc aucun appel réseau"
assert_eq "0" "$(grep -c 'sk-proj-AAAAAAAAAAAAAAAAAAAAAAAA' "$JEV_GUARD_LOG")" \
  "le secret n'atteint pas le journal"
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok"

# Repli quand Jev échoue
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-500"
rc=$(lancer "$(entree Bash '{"command":"rm -rf /"}')")
assert_eq "2" "$rc" "repli bloque rm -rf /"
assert_eq "fallback" "$(jq -r '.source' "$JEV_GUARD_LOG" | tail -1)" "source = fallback"
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok"

# Contrôle déterministe avant tout appel réseau
export JEV_GUARD_CURL=/bin/false
rc=$(lancer "$(entree Write '{"file_path":"/etc/passwd"}')")
assert_eq "2" "$rc" "chemin hors zone bloqué sans réseau"
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok"

# Outil non concerné
rc=$(lancer "$(entree Read '{"file_path":"/projet/a.ts"}')")
assert_eq "0" "$rc" "outil hors périmètre ignoré"

# Cache : deuxième appel identique sans réseau
lancer "$(entree Bash '{"command":"ls -la"}')" >/dev/null
export JEV_GUARD_CURL=/bin/false
rc=$(lancer "$(entree Bash '{"command":"ls -la"}')")
assert_eq "0" "$rc" "second appel servi par le cache"
assert_eq "cache" "$(jq -r '.source' "$JEV_GUARD_LOG" | tail -1)" "source = cache"
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-ok"

# MODE OMBRE : Jev n'a jamais le dernier mot
export JEV_GUARD_MODE=shadow
rc=$(lancer "$(entree Bash '{"command":"git show a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0"}')")
assert_eq "2" "$rc" "mode ombre : la regex décide, donc blocage"
assert_eq "allow" "$(jq -r '.verdict' "$JEV_GUARD_LOG" | tail -1)" "verdict Jev journalisé"
assert_eq "block" "$(jq -r '.regex_verdict' "$JEV_GUARD_LOG" | tail -1)" "verdict regex journalisé"
assert_eq "false" "$(jq -r '.agreed' "$JEV_GUARD_LOG" | tail -1)" "désaccord enregistré"

finish
```

- [ ] **Step 3 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `hooks/jev-guard.sh: No such file or directory`

- [ ] **Step 4 : Écrire l'orchestration**

Créer `hooks/jev-guard.sh` :

```bash
#!/usr/bin/env bash
# Hook PreToolUse unique, adossé à Jev (spec §5).
# Remplace dangerous-actions-blocker.sh et security-check.sh.
#
# Sortie : code 0 = autoriser, code 2 + stderr = bloquer,
#          JSON hookSpecificOutput = demander confirmation.

set -uo pipefail

_LIB=$(cd "$(dirname "${BASH_SOURCE[0]}")/../lib" && pwd)
. "$_LIB/prefilter.sh"
. "$_LIB/log.sh"
. "$_LIB/cache.sh"
. "$_LIB/fallback.sh"
. "$_LIB/deterministic.sh"
. "$_LIB/decide.sh"
. "$_LIB/jev-client.sh"

JEV_GUARD_MODE=${JEV_GUARD_MODE:-shadow}

command -v jq >/dev/null 2>&1 || exit 0

ENTREE=$(cat)
OUTIL=$(jq -r '.tool_name // empty' <<<"$ENTREE" 2>/dev/null)

case "$OUTIL" in
  Bash)        SUJET=$(jq -r '.tool_input.command // empty'   <<<"$ENTREE") ;;
  Edit|Write)  SUJET=$(jq -r '.tool_input.file_path // empty' <<<"$ENTREE") ;;
  *)           exit 0 ;;
esac
[ -n "$SUJET" ] || exit 0

emettre() {
  local verdict=$1 raison=$2
  case "$verdict" in
    block)
      printf 'BLOQUÉ par jev-guard : %s\n' "$raison" >&2
      exit 2 ;;
    ask)
      # Voir Task 8 Step 1 : forme confirmée contre la documentation des hooks.
      jq -nc --arg r "$raison" '{
        hookSpecificOutput: {
          hookEventName: "PreToolUse",
          permissionDecision: "ask",
          permissionDecisionReason: $r
        }}'
      exit 0 ;;
    *)
      exit 0 ;;
  esac
}

REGEX_VERDICT=$(fallback_verdict "$OUTIL" "$SUJET")

# --- Étage 1 : contrôles déterministes (aucun réseau) ---
if [ "$OUTIL" = "Edit" ] || [ "$OUTIL" = "Write" ]; then
  if [ "$(path_containment_verdict "$SUJET")" = "block" ]; then
    log_decision "$OUTIL" "$SUJET" "deterministic" "block" "$REGEX_VERDICT" '{}' null 0
    emettre block "édition hors des zones autorisées : $SUJET"
  fi
  if [ "$REGEX_VERDICT" = "block" ]; then
    log_decision "$OUTIL" "$SUJET" "deterministic" "block" "$REGEX_VERDICT" '{}' null 0
    emettre block "fichier protégé : $(basename "$SUJET")"
  fi
fi

# --- Étage 2 : pré-filtre anti-exfiltration (aucun réseau, spec §7) ---
if MOTIF=$(prefilter_match "$SUJET"); then
  log_decision "$OUTIL" "$SUJET" "prefilter" "block" "$REGEX_VERDICT" '{}' null 0
  emettre block "valeur de secret détectée localement (motif $MOTIF)"
fi

# --- Étage 3 : cache ---
CLE=$(cache_key "$OUTIL" "$SUJET" "$PWD")
if VERDICT=$(cache_get "$CLE"); then
  log_decision "$OUTIL" "$SUJET" "cache" "$VERDICT" "$REGEX_VERDICT" '{}' null 0
  [ "$JEV_GUARD_MODE" = "shadow" ] && VERDICT=$REGEX_VERDICT
  emettre "$VERDICT" "verdict mémorisé"
fi

# --- Étage 4 : Jev ---
DEBUT=$(date +%s)
if [ "$OUTIL" = "Bash" ]; then
  EN_GIT=false; git rev-parse --is-inside-work-tree >/dev/null 2>&1 && EN_GIT=true
  PAYLOAD=$(jev_build_bash_payload "$SUJET" "$PWD" "$EN_GIT")
else
  PAYLOAD=$(jev_build_file_payload "$SUJET" "$OUTIL")
fi

if REPONSE=$(jev_query "$PAYLOAD"); then
  LATENCE=$(( ($(date +%s) - DEBUT) * 1000 ))
  if [ "$OUTIL" = "Bash" ]; then
    VERDICT=$(decide_bash "$REPONSE")
    CONF=$(jq -r '.answers.destructiveness.confidence // null' <<<"$REPONSE")
  else
    VERDICT=$(decide_file "$REPONSE")
    CONF=$(jq -r '.answers.sensitive_file.confidence // null' <<<"$REPONSE")
  fi
  SCORES=$(jq -c '.answers' <<<"$REPONSE")
  cache_put "$CLE" "$VERDICT" "$SCORES"
  log_decision "$OUTIL" "$SUJET" "jev" "$VERDICT" "$REGEX_VERDICT" \
    "$SCORES" "$CONF" "$LATENCE"
else
  # --- Étage 5 : repli ---
  VERDICT=$REGEX_VERDICT
  log_decision "$OUTIL" "$SUJET" "fallback" "$VERDICT" "$REGEX_VERDICT" '{}' null 0
fi

# En mode ombre, Jev est observé mais ne décide jamais.
[ "$JEV_GUARD_MODE" = "shadow" ] && VERDICT=$REGEX_VERDICT

emettre "$VERDICT" "verdict $JEV_GUARD_MODE"
```

Rendre exécutable : `chmod +x hooks/jev-guard.sh`

- [ ] **Step 5 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 6 : Commit**

```bash
git add hooks/jev-guard.sh tests/test_guard.sh
git commit -m "Orchestration du hook en cinq étages

Contrôles déterministes, pré-filtre, cache, Jev, repli. En mode ombre Jev
est interrogé et journalisé mais la regex reste seule décisionnaire."
```

---

### Task 9 : Corpus d'acceptation

Le corpus de la spec §12 est la porte d'entrée de la phase A : il vérifie que Jev corrige effectivement les faux positifs et faux négatifs identifiés, avec des réponses figées qui décrivent le comportement attendu du modèle.

**Files:**
- Create: `tests/fixtures/commands.tsv`
- Create: `tests/fixtures/reponses/*.json`
- Create: `tests/test_corpus.sh`
- Create: `tests/stubs/curl-fixture`

**Interfaces:**
- Consumes: `hooks/jev-guard.sh` (Task 8).
- Produces : rien (tâche de vérification).

- [ ] **Step 1 : Écrire le stub curl paramétrable**

Créer `tests/stubs/curl-fixture` :

```bash
#!/usr/bin/env bash
# Renvoie la réponse figée désignée par JEV_FIXTURE_REPONSE.
cat "$JEV_FIXTURE_REPONSE"
printf '\n200'
```

`chmod +x tests/stubs/curl-fixture`

- [ ] **Step 2 : Écrire les réponses figées**

Créer `tests/fixtures/reponses/anodin.json` :

```json
{"answers":{"secret_exposure":{"noul":0.01},"destructiveness":{"score":0.1,"confidence":0.96},"blast_radius":{"choice":"cwd","confidence":0.95}}}
```

Créer `tests/fixtures/reponses/destructeur-local.json` :

```json
{"answers":{"secret_exposure":{"noul":0.01},"destructiveness":{"score":2.2,"confidence":0.90},"blast_radius":{"choice":"repo","confidence":0.88}}}
```

Créer `tests/fixtures/reponses/destructeur-distant.json` :

```json
{"answers":{"secret_exposure":{"noul":0.01},"destructiveness":{"score":3.0,"confidence":0.95},"blast_radius":{"choice":"shared_remote","confidence":0.93}}}
```

Créer `tests/fixtures/reponses/destructeur-machine.json` :

```json
{"answers":{"secret_exposure":{"noul":0.01},"destructiveness":{"score":3.0,"confidence":0.97},"blast_radius":{"choice":"machine","confidence":0.96}}}
```

- [ ] **Step 3 : Écrire le corpus**

Créer `tests/fixtures/commands.tsv` (séparateur : tabulation, trois colonnes : commande, réponse figée, verdict attendu) :

```
git show a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0	anodin	allow
git cherry-pick a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0	anodin	allow
curl "https://api.example.com/v1/data?token=public"	anodin	allow
md5sum rapport.txt	anodin	allow
ls -la	anodin	allow
find . -delete	destructeur-local	ask
git reset --hard origin/main	destructeur-local	ask
rm -rf "$PROJECT/.."	destructeur-machine	block
git push --force origin feature	destructeur-distant	block
kubectl delete namespace prod	destructeur-distant	block
rm -rf /	destructeur-machine	block
dd if=/dev/zero of=/dev/sda	destructeur-machine	block
npm publish	destructeur-distant	block
git push --force origin main	destructeur-distant	block
```

- [ ] **Step 4 : Écrire le test du corpus (il doit échouer)**

Créer `tests/test_corpus.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"

tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT
export JEV_GUARD_LOG="$tmp/j.jsonl"
export TYPESAFE_API_KEY=cle-de-test
export JEV_GUARD_CURL="$ROOT/tests/stubs/curl-fixture"
export JEV_GUARD_MODE=active
export CLAUDE_PROJECT_DIR=/projet

while IFS=$'\t' read -r commande fixture attendu; do
  [ -n "$commande" ] || continue
  export JEV_FIXTURE_REPONSE="$ROOT/tests/fixtures/reponses/$fixture.json"
  export JEV_GUARD_CACHE_DIR="$tmp/c-$RANDOM"   # cache neuf par cas

  entree=$(jq -nc --arg c "$commande" '{tool_name:"Bash",tool_input:{command:$c}}')
  sortie=$(printf '%s' "$entree" | bash "$ROOT/hooks/jev-guard.sh" 2>/dev/null)
  rc=$?

  case $rc in
    2) obtenu=block ;;
    0) if grep -q '"permissionDecision"' <<<"$sortie"; then obtenu=ask; else obtenu=allow; fi ;;
    *) obtenu="rc=$rc" ;;
  esac

  assert_eq "$attendu" "$obtenu" "${commande:0:50}"
done < "$ROOT/tests/fixtures/commands.tsv"

finish
```

- [ ] **Step 5 : Lancer le test**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC au premier lancement si le corpus révèle un écart de seuillage.

Si un cas échoue, la cause est soit une réponse figée mal calibrée, soit un seuil de `lib/decide.sh` à ajuster. **Ne jamais ajuster le verdict attendu du corpus pour faire passer le test** : le corpus décrit le comportement voulu, c'est le code qui s'y conforme.

- [ ] **Step 6 : Corriger jusqu'au vert, puis commit**

```bash
git add tests/fixtures/ tests/stubs/curl-fixture tests/test_corpus.sh
git commit -m "Corpus d'acceptation sur les faux positifs et négatifs connus

Vérifie de bout en bout que les identifiants git passent et que les
commandes destructrices construites dynamiquement sont rattrapées."
```

---

### Task 10 : Exploitation du journal de phase A

Sans cet outil, les six critères de passage en mode actif (spec §10) ne sont pas mesurables et la phase A ne sert à rien.

**Files:**
- Create: `tools/analyze-shadow-log.sh`
- Create: `tests/test_analyze.sh`

**Interfaces:**
- Consumes: le format JSONL de `log_decision` (Task 2).
- Produces : exécutable prenant un chemin de journal et écrivant le rapport sur stdout.

- [ ] **Step 1 : Écrire le test (il doit échouer)**

Créer `tests/test_analyze.sh` :

```bash
#!/usr/bin/env bash
set -uo pipefail
ROOT=${ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}
. "$ROOT/tests/lib/assert.sh"

tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT
journal="$tmp/j.jsonl"

ligne() {
  jq -nc --arg v "$1" --arg r "$2" --arg s "$3" --argjson l "$4" \
    '{tool:"Bash",cmd_redacted:"x",source:$s,verdict:$v,regex_verdict:$r,
      agreed:($v==$r),latency_ms:$l}' >> "$journal"
}
ligne allow allow jev 100
ligne allow block jev 200
ligne block block jev 150
ligne allow allow fallback 0

rapport=$(bash "$ROOT/tools/analyze-shadow-log.sh" "$journal")

# Extrait la valeur à droite du « : » pour la ligne dont le libellé est donné.
valeur() { sed -n "s/^ *$1 *: *//p" <<<"$rapport" | head -1 | tr -d ' %'; }

assert_eq "4"  "$(valeur 'Décisions totales')"          "compte total"
assert_eq "1"  "$(valeur 'Désaccords')"                 "compte désaccords"
assert_eq "1"  "$(valeur 'Jev autorise, regex bloque')" "désaccord le plus sensible"
assert_eq "0"  "$(valeur 'Jev bloque, regex autorise')" "désaccord inverse"
assert_eq "1"  "$(valeur 'Replis (mode dégradé)')"      "compte replis"
assert_eq "25" "$(valeur 'Taux de repli')"              "taux de repli"

finish
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `./tests/run-tests.sh`
Attendu : ÉCHEC, `tools/analyze-shadow-log.sh: No such file or directory`

- [ ] **Step 3 : Écrire l'outil d'analyse**

Créer `tools/analyze-shadow-log.sh` :

```bash
#!/usr/bin/env bash
# Rapport de phase A (spec §10). Alimente les six critères de passage.
set -uo pipefail

JOURNAL=${1:-${JEV_GUARD_LOG:-$HOME/.claude/logs/jev-guard.jsonl}}
[ -f "$JOURNAL" ] || { printf 'Journal introuvable : %s\n' "$JOURNAL" >&2; exit 1; }

total=$(wc -l < "$JOURNAL" | tr -d ' ')
[ "$total" -gt 0 ] || { printf 'Journal vide\n'; exit 0; }

desaccords=$(jq -s '[.[] | select(.agreed == false)] | length' "$JOURNAL")
jev_allow_regex_block=$(jq -s \
  '[.[] | select(.verdict == "allow" and .regex_verdict == "block")] | length' "$JOURNAL")
jev_block_regex_allow=$(jq -s \
  '[.[] | select(.verdict == "block" and .regex_verdict == "allow")] | length' "$JOURNAL")
replis=$(jq -s '[.[] | select(.source == "fallback")] | length' "$JOURNAL")
taux_repli=$(awk -v r="$replis" -v t="$total" 'BEGIN{printf "%.0f", 100*r/t}')
p95=$(jq -s '[.[] | select(.source=="jev") | .latency_ms] | sort
             | if length == 0 then 0 else .[(length * 0.95 | floor)] end' "$JOURNAL")

cat <<EOF
Rapport de phase A — $JOURNAL

Décisions totales           : $total
Désaccords                  : $desaccords
Jev autorise, regex bloque  : $jev_allow_regex_block
Jev bloque, regex autorise  : $jev_block_regex_allow
Replis (mode dégradé)       : $replis
Taux de repli               : $taux_repli %
Latence Jev p95             : $p95 ms

Critères de passage en mode actif (spec §10) :
  1. >= 200 décisions Bash          : $([ "$total" -ge 200 ] && echo OUI || echo NON)
  2. désaccords bloquants relus     : manuel
  3. aucun allow Jev / block regex justifié : $jev_allow_regex_block à relire
  4. p95 < 800 ms                   : $([ "$p95" -lt 800 ] && echo OUI || echo NON)
  5. taux de repli < 5 %            : $([ "$taux_repli" -lt 5 ] && echo OUI || echo NON)
  6. seuils réajustés               : manuel

Désaccords à relire :
EOF

jq -r 'select(.agreed == false)
       | "  [\(.verdict) vs \(.regex_verdict)] \(.cmd_redacted)"' "$JOURNAL"
```

`chmod +x tools/analyze-shadow-log.sh`

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add tools/analyze-shadow-log.sh tests/test_analyze.sh
git commit -m "Rapport de phase A

Matrice de désaccords, taux de repli et latence p95, adossés aux six
critères de passage en mode actif."
```

---

### Task 11 : Installation en mode ombre

Dernière tâche, et la seule qui touche la configuration vivante de l'utilisateur. Elle est délibérément séparée pour que tout le reste soit vert avant d'approcher `~/.claude/settings.json`.

**Files:**
- Create: `README.md`
- Create: `install.sh`
- Modify: `~/.claude/settings.json` (via `install.sh`, avec sauvegarde)

**Interfaces:**
- Consumes: `hooks/jev-guard.sh` (Task 8).
- Produces : rien.

- [ ] **Step 1 : Écrire le README**

Créer `README.md` :

```markdown
# jev-guard

Hook `PreToolUse` pour Claude Code qui délègue le jugement de sécurité à Jev
(TypeSafe AI), avec pré-filtre anti-exfiltration local et repli sur les
expressions régulières historiques.

- Spec : `docs/superpowers/specs/2026-09-18-jev-security-hooks-design.md`
- Plan : `docs/superpowers/plans/2026-09-18-jev-guard.md`

## Installation

```bash
export TYPESAFE_API_KEY="votre-clé"   # console.typesafe.ai
./install.sh
```

L'installation met le hook en **mode ombre** : Jev est interrogé et journalisé,
mais les expressions régulières restent seules décisionnaires. Rien n'est bloqué
par Jev tant que la phase A n'est pas validée.

## Phase A

Utiliser Claude Code normalement, puis :

```bash
./tools/analyze-shadow-log.sh
```

Les six critères de passage figurent en tête du rapport. Une fois tous remplis
et les désaccords relus, passer en mode actif :

```bash
export JEV_GUARD_MODE=active
```

Retour arrière : remettre `JEV_GUARD_MODE=shadow`, ou `JEV_GUARD_DISABLE=1` pour
court-circuiter Jev entièrement.

## Tests

```bash
./tests/run-tests.sh
```

Aucun test ne touche le réseau.

## Périmètre

Les commandes Bash sont envoyées à l'API TypeSafe. Arbitrage consigné en spec
§13 R1 : accepté dans un cadre POC, hors code client. Utiliser
`JEV_GUARD_DISABLE=1` pour couper par projet.
```

- [ ] **Step 2 : Écrire l'installateur**

Créer `install.sh` :

```bash
#!/usr/bin/env bash
# Installe jev-guard en mode ombre dans ~/.claude/settings.json.
# Sauvegarde le fichier existant et retire les deux hooks remplacés.
set -euo pipefail

RACINE=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
REGLAGES="$HOME/.claude/settings.json"
HOOK="$RACINE/hooks/jev-guard.sh"

[ -f "$REGLAGES" ] || { printf 'Introuvable : %s\n' "$REGLAGES" >&2; exit 1; }

SAUVEGARDE="$REGLAGES.avant-jev-guard.$(date +%Y%m%d%H%M%S)"
cp "$REGLAGES" "$SAUVEGARDE"
printf 'Sauvegarde : %s\n' "$SAUVEGARDE"

jq --arg hook "$HOOK" '
  .hooks.PreToolUse = (
    [ .hooks.PreToolUse[]?
      | .hooks |= map(select(
          (.command // "") | test("dangerous-actions-blocker|security-check") | not))
      | select((.hooks | length) > 0) ]
    + [ { matcher: "", hooks: [ { type: "command", command: $hook, timeout: 10 } ] } ]
  )
' "$SAUVEGARDE" > "$REGLAGES"

printf 'jev-guard installé en mode ombre.\n'
printf 'Vérifier : jq ".hooks.PreToolUse" %s\n' "$REGLAGES"
printf 'Restaurer : cp %s %s\n' "$SAUVEGARDE" "$REGLAGES"
```

`chmod +x install.sh`

- [ ] **Step 3 : Vérifier l'installateur à blanc**

Ne pas lancer `install.sh` sur la vraie configuration. Vérifier d'abord sur une copie :

```bash
mkdir -p /tmp/jev-essai && cp ~/.claude/settings.json /tmp/jev-essai/
HOME=/tmp/jev-essai bash -c 'mkdir -p $HOME/.claude && cp /tmp/jev-essai/settings.json $HOME/.claude/ && cd '"$PWD"' && ./install.sh'
jq '.hooks.PreToolUse' /tmp/jev-essai/.claude/settings.json
```

Attendu : les entrées `dangerous-actions-blocker.sh` et `security-check.sh` ont disparu, une entrée `jev-guard.sh` est présente, les hooks `rtk-*` sont intacts.

- [ ] **Step 4 : Lancer la suite complète**

Run : `./tests/run-tests.sh`
Attendu : `Tous les tests passent`

- [ ] **Step 5 : Commit**

```bash
git add README.md install.sh
git commit -m "Installateur en mode ombre et documentation

L'installation retire les deux hooks remplacés, sauvegarde les réglages
et démarre en mode ombre : rien n'est bloqué par Jev avant validation de
la phase A."
```

- [ ] **Step 6 : Installation réelle — demander confirmation à l'utilisateur**

Cette étape modifie la configuration Claude Code vivante. Ne pas l'exécuter sans accord explicite. Présenter le résultat du Step 3 et demander la validation avant de lancer `./install.sh` sur le vrai `$HOME`.

---

## Self-Review

**Couverture de la spec :**

| Section spec | Tâche |
|---|---|
| §5 arborescence | Tasks 1-11 (plus `lib/decide.sh`, ajout documenté en tête) |
| §6 partage Jev / déterministe | Task 5, Task 7 |
| §7 pré-filtre anti-exfiltration | Task 1, vérifié de bout en bout en Task 8 Step 2 |
| §8.1 format de requête | Task 7 Step 1 (vérification préalable) |
| §8.2 questions typées | Task 7 Step 5 |
| §8.3 seuillage | Task 6 |
| §8.4 Edit/Write | Task 5, Task 6 (`decide_file`), Task 7 |
| §9 repli | Task 4 |
| §9 cache | Task 3 |
| §9 journal | Task 2 |
| §10 phases | Task 8 (mode ombre), Task 10 (critères), Task 11 (défaut ombre) |
| §11 configuration | Tasks 2, 3, 6, 7, 8 (chaque variable a un défaut) |
| §12 tests | Task 9 |
| §13 R6 journal sans secrets | Task 2, test dédié |

Aucune section sans tâche.

**Deux vérifications préalables à ne pas sauter.** Task 7 Step 1 (schéma HTTP réel) et Task 8 Step 1 (protocole `ask`) portent sur des contrats externes non confirmés en session. Les deux sont placés avant le code qui en dépend.

**Cohérence des noms :** `prefilter_match` / `prefilter_redact` (T1), `log_decision` / `_sha256` (T2), `cache_key` / `cache_get` / `cache_put` (T3), `fallback_verdict` (T4), `path_containment_verdict` (T5), `decide_bash` / `decide_file` (T6), `jev_build_bash_payload` / `jev_build_file_payload` / `jev_query` (T7). Chaque nom est employé dans les tâches suivantes sous la forme exacte où il est défini. `_sha256` est défini en T2 et consommé en T3 : `lib/cache.sh` requiert donc que `lib/log.sh` soit sourcé avant lui, ce que `hooks/jev-guard.sh` respecte.
